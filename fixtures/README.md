# Fixtures

Optional example sources used by this repository to exercise C++ formatting and `cppcheck` in CI.

Project code in consumer repositories should follow the same style via the shared [configs/](../configs/) files.
